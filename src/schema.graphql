type Query {
    ad: AdInfo!
    ads: [AdInfo!]!
  }
  
type Mutation {
  post(
    title: String!, 
    text: String!,
    category: String!,
    url: String! 
    ): AdInfo!
}

  type AdInfo {
    id: ID!
    title: String!
    text: String!
    category: String!
    url: String!
  }